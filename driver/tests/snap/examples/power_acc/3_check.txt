def power_acc: fn(x@0: [[u64]], k@1: u64, r@2: [[u64]]) -> [[u64]] {
    lam(x@0: [[u64]], k@1: u64, r@2: [[u64]]) = {
        match k@1 {
            0 => r@2,
            k@3 => {
                let odd@4: u64 = @and_u64(k@3, 1_u64);
                #({
                    let x1@5: u64 = $(x@0);
                    $({
                        match @eq_u64(odd@4, 1_u64) {
                            0 => power_acc(#(@mul_u64(x1@5, x1@5)), @div_u64(k@3, 2_u64), r@2),
                            1 => #($(power_acc(#(@mul_u64(x1@5, x1@5)), @div_u64(k@3, 2_u64), #(@mul_u64($(r@2), x1@5))))),
                        }
                    })
                })
            },
        }
    }
}

def power_acc_1: fn(x@0: [[u64]], k@1: u64) -> [[u64]] {
    lam(x@0: [[u64]], k@1: u64) = {
        match k@1 {
            0 => #(1_u64),
            k@2 => {
                let odd@3: u64 = @and_u64(k@2, 1_u64);
                #({
                    let x1@4: u64 = $(x@0);
                    $({
                        match @eq_u64(odd@3, 1_u64) {
                            0 => power_acc_1(#(@mul_u64(x1@4, x1@4)), @div_u64(k@2, 2_u64)),
                            1 => power_acc(#(@mul_u64(x1@4, x1@4)), @div_u64(k@2, 2_u64), #(x1@4)),
                        }
                    })
                })
            },
        }
    }
}

def power: fn(x@0: [[u64]], k@1: u64) -> [[u64]] {
    lam(x@0: [[u64]], k@1: u64) = power_acc_1(x@0, k@1)
}

code def pow1(x@0: u64) -> u64 {
    $(power(#(x@0), 1_u64))
}

code def pow5(x@0: u64) -> u64 {
    $(power(#(x@0), 5_u64))
}

code def pow11_inc(x@0: u64) -> u64 {
    $(power(#({
        let y@1: u64 = @add_u64(x@0, 1_u64);
        y@1
    }), 11_u64))
}

code def pow15(x@0: u64) -> u64 {
    $(power(#(x@0), 15_u64))
}

code def pow16(x@0: u64) -> u64 {
    $(power(#(x@0), 16_u64))
}

code def pow17(x@0: u64) -> u64 {
    $(power(#(x@0), 17_u64))
}

