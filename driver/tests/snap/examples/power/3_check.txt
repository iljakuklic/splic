def power(x@0: [[u64]], exp@1: u64) -> [[u64]] {
    lam(x@2: [[u64]], exp@3: u64) = {
        match exp@3 {
            0 => #(1_u64),
            1 => x@2,
            exp@4 => {
                let exp2@5: [[u64]] = #({
                    let x2@5: u64 = $(power(x@2, @div_u64(exp@4, 2_u64)));
                    @mul_u64(x2@5, x2@5)
                });
                let odd@6: u64 = @and_u64(exp@4, 1_u64);
                match @eq_u64(odd@6, 1_u64) {
                    0 => exp2@5,
                    1 => #(@mul_u64($(exp2@5), $(x@2))),
                }
            },
        }
    }
}

code def pow5(x@0: u64) -> u64 {
    $(power(#(x@0), 5_u64))
}

code def pow11(x@0: u64) -> u64 {
    $(power(#(x@0), 11_u64))
}

