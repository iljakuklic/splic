def power(x@0: [[u64]], exp@1: u64) -> [[u64]] {
    lam(x@2: [[u64]], exp@3: u64) = {
        match exp@3 {
            0 => #(1_u64),
            1 => x@2,
            exp@4 => #(@mul_u64($(power(x@2, @sub_u64(exp@4, 1_u64))), $(x@2))),
        }
    }
}

code def pow5(x@0: u64) -> u64 {
    $(power(#(x@0), 5_u64))
}

code def pow11(x@0: u64) -> u64 {
    $(power(#(x@0), 11_u64))
}

