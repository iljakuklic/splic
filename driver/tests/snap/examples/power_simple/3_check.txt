def power: fn(x@0: [[u64]], exp@1: u64) -> [[u64]] {
    lam(x@0: [[u64]], exp@1: u64) = {
        match exp@1 {
            0 => #(1_u64),
            1 => x@0,
            exp@2 => #(@mul_u64($(power(x@0, @sub_u64(exp@2, 1_u64))), $(x@0))),
        }
    }
}

code def pow5(x@0: u64) -> u64 {
    $(power(#(x@0), 5_u64))
}

code def pow11(x@0: u64) -> u64 {
    $(power(#(x@0), 11_u64))
}

