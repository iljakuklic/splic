// Higher-order staging: meta function that transforms code via a lambda
def map_code(f: fn(_: [[u64]]) -> [[u64]], x: [[u64]]) -> [[u64]] = {
    f(x)
};

code def double(x: u64) -> u64 = {
    $(map_code(lam(y: [[u64]]) = #($(y) + $(y)), #(x)))
};
