def apply_twice: fn(A@0: Type, f@1: fn(_: A@0) -> A@0, x@2: A@0) -> A@0 {
    lam(A@0: Type, f@1: fn(_: A@0) -> A@0, x@2: A@0) = f@1(f@1(x@2))
}

def inc: fn(x@0: u64) -> u64 {
    lam(x@0: u64) = @add_u64(x@0, 1_u64)
}

def test: fn() -> u64 {
    lam() = apply_twice(u64, inc, 0_u64)
}

code def result() -> u64 {
    $(@embed_u64(test()))
}

