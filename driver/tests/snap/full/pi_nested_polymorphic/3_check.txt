fn apply_twice(A@0: Type, f@1: fn(_: A@0) -> A@0, x@2: A@0) -> A@0 {
    f@1(f@1(x@2))
}

fn inc(x@0: u64) -> u64 {
    @add_u64(x@0, 1_u64)
}

fn test() -> u64 {
    apply_twice(u64, inc, 0_u64)
}

code fn result() -> u64 {
    $(@embed_u64(test()))
}

