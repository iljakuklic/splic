// Polymorphic apply_twice: polymorphic in both the value type and the function
def apply_twice(A: Type, f: fn(_: A) -> A, x: A) -> A = {
    f(f(x))
};

def inc(x: u64) -> u64 = { x + 1 };

def test() -> u64 = {
    apply_twice(u64, inc, 0)
};

code def result() -> u64 = { $(test()) };
