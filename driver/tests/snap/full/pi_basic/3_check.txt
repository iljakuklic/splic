def apply(f@0: fn(_: u64) -> u64, x@1: u64) -> u64 {
    lam(f@2: fn(_: u64) -> u64, x@3: u64) = f@2(x@3)
}

def inc(x@0: u64) -> u64 {
    lam(x@1: u64) = @add_u64(x@1, 1_u64)
}

def test() -> u64 {
    lam() = apply(inc, 42_u64)
}

code def result() -> u64 {
    $(@embed_u64(test()))
}

