// Higher-order function: pass a function as an argument
def apply(f: fn(_: u64) -> u64, x: u64) -> u64 = {
    f(x)
};

def inc(x: u64) -> u64 = { x + 1 };

def test() -> u64 = {
    apply(inc, 42)
};

code def result() -> u64 = { $(test()) };
