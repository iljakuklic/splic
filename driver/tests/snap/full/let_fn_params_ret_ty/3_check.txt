fn test() -> u64 {
    lam() = {
        let inc@0: fn(x@0: u64) -> u64 = lam(x@0: u64) = @add_u64(x@0, 1_u64);
        inc@0(41_u64)
    }
}

code fn result() -> u64 {
    lam() = $(@embed_u64(test()))
}

