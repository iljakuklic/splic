// A lambda with an empty parameter list creates a thunk
def make_thunk(x: u64) -> fn() -> u64 = {
    lam() = x
};

def test() -> u64 = {
    make_thunk(42)()
};

code def result() -> u64 = { $(test()) };
