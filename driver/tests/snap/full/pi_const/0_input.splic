// Const combinator: returns a function that ignores its argument
def const_(A: Type, B: Type) -> fn(_: A) -> fn(_: B) -> A = {
    lam(a: A) = lam(b: B) = a
};

def test() -> u64 = {
    const_(u64, u8)(42)(7)
};

code def result() -> u64 = { $(test()) };
