def const_: fn(A@0: Type, B@1: Type) -> fn(_: A@0) -> fn(_: B@1) -> A@0 {
    lam(A@0: Type, B@1: Type) = lam(a@2: A@0) = lam(b@3: B@1) = a@2
}

def test: fn() -> u64 {
    lam() = const_(u64, u8)(42_u64)(7_u8)
}

code def result() -> u64 {
    $(@embed_u64(test()))
}

