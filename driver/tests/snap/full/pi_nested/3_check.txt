def apply_twice: fn(f@0: fn(_: u64) -> u64, x@1: u64) -> u64 {
    lam(f@0: fn(_: u64) -> u64, x@1: u64) = f@0(f@0(x@1))
}

def inc: fn(x@0: u64) -> u64 {
    lam(x@0: u64) = @add_u64(x@0, 1_u64)
}

def test: fn() -> u64 {
    lam() = apply_twice(inc, 0_u64)
}

code def result() -> u64 {
    $(@embed_u64(test()))
}

