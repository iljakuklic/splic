// Nested function types: function that takes a function and applies it twice
def apply_twice(f: fn(_: u64) -> u64, x: u64) -> u64 = {
    f(f(x))
};

def inc(x: u64) -> u64 = { x + 1 };

def test() -> u64 = {
    apply_twice(inc, 0)
};

code def result() -> u64 = { $(test()) };
