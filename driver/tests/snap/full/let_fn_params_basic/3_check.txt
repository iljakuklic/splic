fn test() -> u64 {
    lam() = {
        let fst@0: fn(x@0: u64, y@1: u64) -> u64 = lam(x@0: u64, y@1: u64) = x@0;
        let add@1: fn(x@1: u64, y@2: u64) -> u64 = lam(x@1: u64, y@2: u64) = @add_u64(x@1, y@2);
        add@1(fst@0(10_u64, 99_u64), 32_u64)
    }
}

code fn result() -> u64 {
    $(@embed_u64(test()))
}

