def compose: fn(f@0: fn(_: u64) -> u64, g@1: fn(_: u64) -> u64) -> fn(_: u64) -> u64 {
    lam(f@0: fn(_: u64) -> u64, g@1: fn(_: u64) -> u64) = lam(x@2: u64) = f@0(g@1(x@2))
}

def double: fn(x@0: u64) -> u64 {
    lam(x@0: u64) = @add_u64(x@0, x@0)
}

def inc: fn(x@0: u64) -> u64 {
    lam(x@0: u64) = @add_u64(x@0, 1_u64)
}

def test: fn() -> u64 {
    lam() = compose(double, inc)(5_u64)
}

code def result() -> u64 {
    $(@embed_u64(test()))
}

