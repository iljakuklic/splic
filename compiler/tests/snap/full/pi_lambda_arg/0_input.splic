// Pass a lambda as an argument to a higher-order function
fn apply(f: fn(u64) -> u64, x: u64) -> u64 {
    f(x)
}

fn test() -> u64 {
    apply(|x: u64| x + 1, 42)
}

code fn result() -> u64 { $(test()) }
